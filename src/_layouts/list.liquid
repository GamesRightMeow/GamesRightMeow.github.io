---
layout: garden
tags: list
---

{{ content }}

<ul>
{% assign pages = collections.garden | withTag: displayCollection | withoutTag: "list" | sort: "data.title" %}
{%- for page in pages -%}
  <li><a href="{{ page.url }}">{{ page.data.title }}</a></li>
{%- endfor -%}
</ul>
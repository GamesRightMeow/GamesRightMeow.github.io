<ul>  
  {% for article in collections.recentlyPlanted limit:count %}
  <li>
    <a href="{{article.url}}">{{ article.data.status | prettyStatusIcon }} {{ article.data.title }}</a>
    ({{ article.data.planted | timeSince }})
  </li>
  {% endfor %}
</ul>